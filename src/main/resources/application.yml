server:
  port: 8080

spring:
  application:
    name: wallet-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-dahrnffqj5pc73a9ep1g-a:5432/wallet_aors}
    username: ${DB_USER:user1}
    password: ${DB_PASSWORD:hS8N0VrDdv7KhKAhPhdMHEOGrBJbgiCU}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-change-me-please-32-bytes-minimum-for-hs256}
    expiration-ms: ${JWT_EXPIRATION_MS:3600000}

logging:
  level:
    root: INFO
    com.wallet: INFO
